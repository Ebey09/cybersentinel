# Test fixtures

Synthetic files only. Values are generated or hand-written to exercise code paths.
They must not be copied from, or derived row-by-row from, any real dataset.
Phase 1 tests build their inputs in code, so this folder is empty for now.
