# Results

Committed evaluation outputs (metrics JSON, plots, parity evidence). Small files only.
Every file must be traceable to a run id, an experiment config SHA-256 and dataset SHA-256s.
Empty until Phase 5.
